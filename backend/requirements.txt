# SignEase backend — deliberately minimal. No tensorflow: it drags in a protobuf
# version that conflicts with mediapipe in this environment. We only need torch.
torch>=2.7.0
mediapipe>=0.10.18
opencv-python-headless>=4.8.0
numpy>=1.26.0,<2.0.0
pandas>=2.0.0
pyarrow>=15.0.0
scikit-learn>=1.3.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
httpx>=0.27.0
python-dotenv>=1.0.0
kaggle>=1.6.0
tqdm>=4.66.0

# Local fallback tier for the audio pipeline (Sarvam is primary; these two
# keep /api/speak and /api/transcribe working even if Sarvam is unreachable,
# with zero extra API keys/cost since both run entirely on-device).
piper-tts>=1.6.0
faster-whisper>=1.0.0
